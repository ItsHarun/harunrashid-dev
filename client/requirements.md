## Packages
cmdk | Fast, unstyled command menu for the command palette
react-markdown | To render markdown content for posts
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  mono: ["var(--font-mono)"],
}
